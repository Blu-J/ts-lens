language: node_js
node_js:
  - "node"
notifications:
  email:
    on_success: never
    on_failure: always
  
cache: 
  yarn: true
  directories:
    - typings
  
script:
  - npm run lint && npm run build && npm run test
after_success:
  - npm run semantic-release